# What to track. Edit freely -- this is the only file you need to touch day to day.
#
# Every product inherits `defaults` and can override any key.
# Matching is done on the normalised listing title: punctuation is stripped and
# case is ignored, so "OP-17", "OP 17" and "op17" are all compared the same way.
# Terms match whole words only, so "case" will not fire inside "showcase".

defaults:
  # Sanity bounds passed to eBay. Anything outside is never even returned.
  min_price: 35
  max_price: 500

  # Must be an actual BOOSTER box. Requiring only "box" matched Illustration
  # Boxes and DP-11 Double Packs, which live in the same eBay category and
  # dragged the median from ~$227 down to ~$55.
  require_all:
    - booster box

  # Reject anything that is not a plain sealed English booster box.
  exclude_any:
    # Wrong language printing
    - japanese
    - japan
    - jpn
    - jp
    - korean
    - chinese
    # Not a sealed box
    - empty
    - open
    - opened
    - unsealed
    - resealed
    - bulk
    - illustration box
    - double pack
    - wrapper
    - wrappers
    - single
    - singles
    - starter deck
    - deck box
    - sleeve
    - sleeves
    - playmat
    - binder
    # Group breaks and gambling formats, not a product you receive
    - break
    - breaks
    - spot
    - spots
    - random
    - repack
    - repacks
    - mystery
    # Counterfeit / not-the-real-thing signals
    - proxy
    - proxies
    - custom
    - replica
    - fake
    # Different unit of sale -- a case is 12 boxes and wrecks price comparison
    - case
    - lot

  # ---- VETOES: any one of these disqualifies a listing outright ----------

  min_seller_score: 100        # minimum completed sales
  min_seller_pct: 98.0         # minimum positive feedback percentage
  require_returns: false       # SCORED, not vetoed -- see note below
  require_us_location: true    # reject items shipping from outside the US
  # Live calibration 2026-08-11: require_returns=true vetoed 8/8 OP-16 and 6/8
  # OP-14 listings. No-returns is standard for sealed TCG (it blocks buyers
  # swapping contents and returning), so vetoing on it rejected the whole
  # market. It still costs 12 points in the trust score.
  require_verified_detail: true  # reject anything whose details will not load

  # Hard price floor, applied no matter how few listings came back. A newly
  # released set often has only two or three listings, and that is exactly when
  # a counterfeit would otherwise be crowned "cheapest". Set to a price no
  # genuine sealed box of that set would ever go below (~55-65% of market).
  implausible_below: null

  # A seller holding this many sealed boxes of a hot set is a distributor or a
  # problem. Above it, the stock-level signal scores zero (not a veto).
  max_quantity: 12

  # ---- SCORE: listings must clear this to be recommended -----------------

  # 0-100, weighted across language verification, seller volume and rating,
  # returns, Top Rated status, price plausibility, item specifics, category,
  # stock level, eBay programmes and description. Raise it to be stricter;
  # 85+ will reject most listings, which is a legitimate choice.
  min_trust_score: 70

  # How many of the cheapest relevant listings get a full detail fetch and
  # score. Each costs one API call. The cheapest are both the ones you would
  # buy and the ones most likely to be fake, so checking those is what matters.
  verify_top_n: 8

  # ---- PURCHASE SCORING --------------------------------------------------

  unit_kind: box               # box | pack -- changes how cost/pack is derived
  packs_in_unit: 24            # English booster box = 24 packs

  # Estimated singles value per pack, from community pull rates. Bandai
  # publishes none, so this is an assumption made explicit rather than left in
  # your gut. Worked example for a typical set, per 24-pack box:
  #     SP    0.2/box x ~$150 avg  =  $30   (1 SP per 5-6 boxes; most SPs are
  #                                          NOT the $650 chase card)
  #     SR    7/box   x ~$8   avg  =  $56
  #     bulk  rares/uncommons      =  $30
  #                                 -------
  #                            box   $116  ->  ~$4.83 per pack
  # Against ~$8.75/pack for a $210 box, that is the arithmetic reason opening
  # boxes loses money on average. Adjust per set as you learn real numbers.
  ev_per_pack: 4.83

  # SPs per box. Community estimates cluster around 1 per 12-box case (~0.083)
  # up to 1 per 5-6 boxes (~0.2) depending on set. Using the pessimistic end,
  # because the point of this number is to show how often a box is a dud.
  #
  # NOTE ON "CASE MAPPING": in older Pokemon sets, hits sat in predictable
  # positions within a case, so a broken case left genuinely worthless boxes.
  # Community consensus is that this does NOT hold for One Piece -- the
  # distribution is genuinely variable (people pull 2 SECs in one box then
  # nothing for three). There is no documented date when Bandai "fixed" it,
  # because there is no evidence One Piece was ever reliably mappable.
  # What IS true is the base rate: most boxes contain no SP. That is what
  # dud_chance shows.
  sp_per_box: 0.12

  # P(pulling ANY of the set's top-8 chase cards from one box). Alt arts turn
  # up reasonably often; manga rares are the tail. 0.30 is a community-informed
  # guess, not a published figure -- it only scales the modelled profit chance,
  # never the arithmetic upside figures beside it.
  chase_hit_rate: 0.30

  # Sellers you have bought from successfully (bonus points), and sellers you
  # never want to see again (instant veto). Use exact eBay usernames.
  trusted_sellers: []
  blocked_sellers: []

products:
  - id: op01
    name: "OP-01 Romance Dawn"
    query: "One Piece Card Game OP-01 Booster Box"
    require_any: ['op 01', 'op01', 'op1', 'romance dawn']
    min_price: 120
    max_price: 1200
    implausible_below: 180
    verify_top_n: 5
    alert_below: null

  - id: op02
    name: "OP-02 Paramount War"
    query: "One Piece Card Game OP-02 Booster Box"
    require_any: ['op 02', 'op02', 'op2', 'paramount war']
    min_price: 100
    max_price: 900
    implausible_below: 140
    verify_top_n: 5
    alert_below: 505.99

  - id: op03
    name: "OP-03 Pillars of Strength"
    query: "One Piece Card Game OP-03 Booster Box"
    require_any: ['op 03', 'op03', 'op3', 'pillars of strength']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: 558.25

  - id: op04
    name: "OP-04 Kingdoms of Intrigue"
    query: "One Piece Card Game OP-04 Booster Box"
    require_any: ['kingdoms of intrigue', 'op 04', 'op04', 'op4']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: 469.2

  - id: op05
    name: "OP-05 Awakening of the New Era"
    query: "One Piece Card Game OP-05 Booster Box"
    require_any: ['awakening of the new era', 'op 05', 'op05', 'op5']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: null

  - id: op06
    name: "OP-06 Wings of the Captain"
    query: "One Piece Card Game OP-06 Booster Box"
    require_any: ['op 06', 'op06', 'op6', 'wings of the captain']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: 392.84

  - id: op07
    name: "OP-07 500 Years Into the Future"
    query: "One Piece Card Game OP-07 Booster Box"
    require_any: ['500 years in the future', '500 years into the future', 'op 07', 'op07', 'op7']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: 374.25

  - id: op08
    name: "OP-08 Two Legends"
    query: "One Piece Card Game OP-08 Booster Box"
    require_any: ['op 08', 'op08', 'op8', 'two legends']
    min_price: 80
    max_price: 700
    implausible_below: 110
    verify_top_n: 5
    alert_below: 275.08

  - id: op09
    name: "OP-09 Emperors in the New World"
    query: "One Piece Card Game OP-09 Booster Box"
    require_any: ['emperors in the new world', 'op 09', 'op09', 'op9']
    min_price: 70
    max_price: 600
    implausible_below: 95
    verify_top_n: 5
    alert_below: null

  - id: op10
    name: "OP-10 Royal Blood"
    query: "One Piece Card Game OP-10 Booster Box"
    require_any: ['op 10', 'op10', 'royal blood']
    min_price: 70
    max_price: 600
    implausible_below: 95
    verify_top_n: 5
    alert_below: 275.08

  - id: op11
    name: "OP-11 A Fist of Divine Speed"
    query: "One Piece Card Game OP-11 Booster Box"
    require_any: ['a fist of divine speed', 'fist of divine speed', 'op 11', 'op11']
    min_price: 70
    max_price: 600
    implausible_below: 95
    verify_top_n: 5
    alert_below: null

  - id: op12
    name: "OP-12 Legacy of the Master"
    query: "One Piece Card Game OP-12 Booster Box"
    require_any: ['legacy of the master', 'op 12', 'op12']
    min_price: 70
    max_price: 600
    implausible_below: 95
    verify_top_n: 5
    alert_below: 312.86

  - id: op13
    name: "OP-13 Carrying On His Will"
    query: "One Piece Card Game OP-13 Booster Box"
    require_any: ['carrying on his will', 'op 13', 'op13']
    min_price: 70
    max_price: 600
    implausible_below: 95
    verify_top_n: 5
    alert_below: 522.56

  - id: op14
    name: "OP-14 The Azure Sea's Seven"
    query: "One Piece Card Game OP-14 Booster Box"
    require_any: ['azure sea s seven', 'azure seas seven', 'op 14', 'op14']
    min_price: 70
    max_price: 600
    implausible_below: 178
    verify_top_n: 5
    alert_below: 266.8

  - id: op15
    name: "OP-15 The Adventure on God's Island"
    query: "One Piece Card Game OP-15 Booster Box"
    require_any: ['op 15', 'op15', 'adventure on god s island', 'adventure on gods island']
    # A seller labelled a Memorial Collection box "(OP-15)" and it hijacked this
    # slot at $390 while real OP-15 boxes sat at $95 in the rejections. Memorial
    # Collection is EB-01 and has its own entry.
    exclude_any: ['memorial collection', 'japanese', 'japan', 'jpn', 'jp', 'korean', 'chinese',
                  'empty', 'open', 'opened', 'unsealed', 'resealed', 'bulk', 'illustration box',
                  'double pack', 'wrapper', 'single', 'singles', 'starter deck', 'deck box',
                  'sleeve', 'sleeves', 'playmat', 'binder', 'break', 'breaks', 'spot', 'spots',
                  'random', 'repack', 'mystery', 'proxy', 'custom', 'replica', 'fake', 'case', 'lot']
    min_price: 60
    max_price: 600
    implausible_below: 70
    verify_top_n: 5
    alert_below: 367.99

  - id: op16
    name: "OP-16 The Time of Battle"
    query: "One Piece Card Game OP-16 Booster Box"
    require_any: ['op 16', 'op16', 'time of battle']
    min_price: 70
    max_price: 600
    implausible_below: 100
    verify_top_n: 8
    alert_below: 193.19

  - id: op17
    name: "OP-17 The World's Strongest Warriors"
    query: "One Piece Card Game OP-17 Booster Box"
    require_any: ['op 17', 'op17', 'world s strongest warriors', 'worlds strongest warriors']
    min_price: 70
    max_price: 600
    implausible_below: 75
    verify_top_n: 8
    alert_below: null

  - id: eb01
    name: "EB-01 Memorial Collection"
    query: "One Piece Card Game EB-01 Booster Box"
    require_any: ['eb 01', 'eb01', 'eb1', 'memorial collection']
    min_price: 40
    max_price: 400
    implausible_below: 50
    verify_top_n: 5
    alert_below: 370.76

  - id: eb02
    name: "EB-02 Anime 25th Collection"
    query: "One Piece Card Game EB-02 Booster Box"
    require_any: ['anime 25th', 'eb 02', 'eb02', 'eb2']
    min_price: 40
    max_price: 400
    implausible_below: 50
    verify_top_n: 5
    alert_below: null

  - id: eb03
    name: "EB-03 Heroines Edition"
    query: "One Piece Card Game EB-03 Booster Box"
    require_any: ['eb 03', 'eb03', 'eb3', 'heroines edition']
    min_price: 40
    max_price: 400
    implausible_below: 50
    verify_top_n: 5
    alert_below: null

  - id: prb01
    name: "PRB-01 One Piece Card The Best"
    query: "One Piece Card Game PRB-01 Booster Box"
    require_any: ['card the best', 'prb 01', 'prb01', 'prb1']
    min_price: 40
    max_price: 500
    implausible_below: 50
    verify_top_n: 5
    alert_below: null

  - id: prb02
    name: "PRB-02 One Piece Card The Best Vol.2"
    query: "One Piece Card Game PRB-02 Booster Box"
    require_any: ['card the best vol 2', 'prb 02', 'prb02', 'prb2']
    min_price: 40
    max_price: 500
    implausible_below: 50
    verify_top_n: 5
    alert_below: 367.95

# Set codes are the reliable matcher; names are a convenience. Always include
# both the spaced and unspaced form. implausible_below values below are rough
# floors -- run `python -m tracker --calibrate` and tighten them from real data.
